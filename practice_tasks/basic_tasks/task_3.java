package practice_tasks.basic_tasks;

public class task_3 {
    public static void main(String[] args) {
        boolean isnotequal = (100 != 200); // true
        boolean isgreater = (100 > 50); // true
        boolean islesser = (100 < 200); // true
        System.out.println("Is 100 not equal to 200?\n " + isnotequal);
        System.out.println("Is 100 greater than 50?\n " + isgreater);
        System.out.println("Is 100 less than 200?\n " + islesser);    
    }
}
